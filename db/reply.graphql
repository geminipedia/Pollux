type Reply {
  id: UUID! @unique @id       # System only
  replyId: Int! @unique       # Human-readable format
# ---
  content: String!
  user: User!
  createdAt: DateTime!
  updatedAt: DateTime!
# ---
  replies: [Reply!]
# ---
  meta: Json
}
