interface Article {
  id: UUID! @unique @id
  title: String!
  published: Boolean! @default(value: false)
  createdAt: DateTime!
  updatedAt: DateTime!
  author: User!
  banner: [Image!]
  meta: Json
}
